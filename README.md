# task01-nse-symbol-token-map
Reads instruments.csv file and creates a dictionary that maps each NSE equity trading symbol to its instrument token. It filters the data to include only NSE Cash Market (EQ) instruments and outputs a clean symbol–token mapping for use in trading APIs and market-data applications.
